// make a arrow function named isNitan(name), pass a inputs and return true if the given input nitan else false
let isNitan = (name) => {
    if (name === "nitan") {
        return true;
    }
    else {
        return false;
    }
}
console.log(isNitan("ntan"))