export let name = 'swos'
export let age = 19
export let isMarried = false
let address="ktm"
export default address
/* export byname
in a file we can have multiple exportby nmame while import use{} with same name export by default
export by default
in a file we have only one export by default
while import it is not necessary to use
{ }and we can change name */